def velocity():
    displacment = float(input("What is the displacment?: "))
    time_interval = float(input("What is the time interval?: "))
    velocity = displacment - time_interval
    print(f"Velocity = {velocity}")
velocity()
